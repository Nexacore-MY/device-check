# Secrets — never commit these
gcp-vision-key.json
*.key.json
.env
.env.local
.env.*.local
admin-config.js

# Admin tools — admin.html itself is hostable now (requires token at runtime).
# admin-config.js remains gitignored because it contains the admin token.

# Personal/business files that don't belong in the repo
squarespace*.pdf
*Demo Talking Points*.docx

# Internal sales / prep docs
Device Check - Demo Talking Points.docx
COMMERCIAL-READINESS.md
squarespace*.pdf

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artefacts
dist/
build/
.next/
.vercel/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*.bak
